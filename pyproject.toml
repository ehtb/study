[project]
name = "transformer"
version = "0.1.0"
description = "Jupyter notebooks exploring language models — from bigram to GPT"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.14.0",
    "datasets>=5.0.0",
    "jupyter>=1.1.1",
    "matplotlib>=3.8.0",
    "numpy>=2.4.6",
    "peft>=0.19.1",
    "scikit-learn>=1.4.0",
    "torch>=2.13.0",
    "transformers>=5.14.0",
    "trl>=1.8.0",
]
